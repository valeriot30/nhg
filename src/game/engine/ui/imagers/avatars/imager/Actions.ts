import AvatarImageData from "./AvatarImageData";

export default class Actions {
    private data: AvatarImageData;

    constructor(data: AvatarImageData) {
        this.data = data;
    }

    //AvatarActions.ts actions: { [key: string] : Action }
}
